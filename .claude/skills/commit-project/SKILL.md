---
name: commit-project
description: Cria commits no padrao do template-servico-python (Conventional Commits em pt-BR).
disable-model-invocation: true
---

# Commit no padrao template-servico-python

## Formato

```
<tipo>(<escopo>): <descricao em pt-BR>
```

- **tipo:** `feat`, `fix`, `chore`, `refactor`, `docs`, `test`.
- **escopo:** `api`, `auth`, `db`, `infra`, `schemas`, `worker`.
